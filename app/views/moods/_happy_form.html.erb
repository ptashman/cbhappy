<%= form_for @mood, url: url_for(controller: 'moods', action: 'happify') do |f| %>
  <% if @mood.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mood.errors.count, "error") %> prohibited this mood from being saved:</h2>

      <ul>
      <% @mood.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit("", type: :image, src: "/assets/happy.png", id: "happy") %>
  </div>
<% end %>
